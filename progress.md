# suzune — Progress

Local-first, open-source alternative to Wispr Flow. Multi-session project; this file plus tasks.json plus git log is the resume state. On "resume tasks": read this file, tasks.json, and `git log --oneline -20` before doing anything.

## Phase status

| Phase | Status | Output |
|---|---|---|
| 1. Research (Wispr Flow teardown, complaints, local ASR stacks) | DONE 2026-07-03 — 3 reports + docs/findings.md | docs/ |
| 2. Planning | DONE 2026-07-03 — plan approved (decisions D1-D5) | implementation_plan.md |
| 3. Implementation (M0-M4 + injection fix, settings UI, first-run download) | DONE — see milestone log below. Two M3 items remain deferred: waveform level metering, engine-load stagger (not blocking; app is fully functional without them) | app code in this repo |
| 4. Landing page | DONE — self-contained landing/index.html, visually verified desktop + 390px | landing/ |
| 5. Demo video + LinkedIn post | DONE — video re-rendered post-rename; LinkedIn post drafted and held per user (not posted) | docs/demo/suzune-demo.mp4, docs/linkedin-post.md |
| 6. Model cleanup + grammar/tone modes + personalization (M7 planned, not built) | DONE — see 2026-07-07 handoff below | src-tauri/, docs/spike-results.md |

## Key context for future sessions

- Local reference repos (read `brain/repos/index.md`): `repos/Handy` — offline dictation, Tauri + Rust, whisper-rs + Parakeet (transcribe-rs), Silero VAD (vad-rs), cpal audio, rdev shortcuts. `repos/voicebox` — MLX GPU acceleration on Apple Silicon, TS/Python/Rust. Study Handy before architecting; it is 90% of the problem already solved.
- Target hardware: MacBook M1 Pro. Local models live at `/Volumes/1TB SSD/LM` (HF_HOME).
- UI: paperback theme — warm paper surfaces, visible grid, serif/literary type, light/dark toggle, no Inter/Roboto/Arial. Floating always-on-top indicator: minimal, smoothly animated.
- Deliverables checklist: (1) working app in raw/suzune pushed to GitHub, (2) 3D animated landing page, (3) demo video, (4) LinkedIn post.
- Safety: confirm before force-push / history rewrite / making repo public. Routine commits fine.
- SSD spawns AppleDouble `._*` files — delete before staging (`find . -name '._*' -delete`).

## Handoff notes

2026-07-03, session 1 (phase 3 underway):
- Plan approved (all recommendations D1-D5 accepted: greenfield, model download on first run, macOS-only v1, no privacy-incident citations in marketing, private repo first).
- M0 DONE: Rust 1.96.1 installed; Tauri 2 + React-TS scaffold builds; cargo target-dir on internal disk (~/.cache/cargo-targets/suzune) because SSD AppleDouble files corrupt tauri build output — keep .cargo/config.toml git-ignored.
- Spikes DONE, see docs/spike-results.md. Binding decisions: Parakeet v2 int8 CPU default (RTF 0.035); model stays resident (17s reload); cleanup LLM = Qwen3-4B-Instruct-2507 Q4_K_M via llama-server (Metal), prompt v2 in spikes/s3-cleanup-bench/bench.py, prompt v3 must add no-code-conversion rule.
- Models on disk at /Volumes/1TB SSD/LM/suzune-models/: parakeet-tdt-0.6b-v2-int8/, ggml-large-v3-turbo.bin, Qwen3-4B-Instruct-2507-Q4_K_M.gguf (+ rejected Qwen3-1.7B, Llama-3.2-3B GGUFs).
- llama.cpp installed via Homebrew (llama-server on PATH).
- M1 DONE (commit 594ba6f): audio/vad/asr crates + pipeline-cli verified end to end on real audio (jfk.wav -> correct transcript, 125ms ASR; live mic opens/times out cleanly). Silero v4 not v5 (vad-rs hardcodes v4 graph — documented in crates/vad). transcribe-rs 0.3.11 fixed the 17s Parakeet load (now ~1s).
- Design note from M1: push-to-talk must transcribe the WHOLE buffer (VAD only trims edges); VAD endpointing (~1s+ trailing) is for hands-free toggle mode only — 700ms cuts at rhetorical pauses.
- M2 DONE (commits d1c9e3c, 8375360): cleanup crate (llama-server manager + client + SYSTEM_PROMPT_V3, real-model tests pass), inject crate (AxInsert -> ClipboardPaste chain, typed errors), coordinator + global shortcut wired (push-to-talk alt+space default, toggle mode, dictation-state events, cancel_dictation command). App boots clean end to end.
- BLOCKED ON USER: macOS Accessibility permission for the terminal (System Settings > Privacy & Security > Accessibility) — needed for the live full-loop test (speak -> text lands in a focused field). Also expect a one-time Microphone prompt.
- NEXT (M3): paperback UI — floating overlay window (always-on-top, non-activating, states: idle/recording waveform/processing shimmer, listens to dictation-state events), settings window (shortcut, engine, cleanup toggle, model manager, history), light/dark. Use frontend-design skill; verify-live at desktop + ~390px. Then M4 polish/README/GitHub push (private), M5 landing page, M6 demo video + LinkedIn post.
- Settings file materializes at ~/Library/Application Support/dev.suzune.desktop/settings.json (models_root defaults to /Volumes/1TB SSD/LM/suzune-models on this machine).
- M3 CORE DONE (commit ac8f9de): tray-only app, paperback overlay pill (visually verified), read-only settings page. BUILD RULE: always `bun run tauri build --no-bundle` — plain `cargo build` produces a binary whose webviews load about:blank. Overlay window must stay listed in src-tauri/capabilities/default.json. Cleanup model is now Qwen2.5-1.5B (e96fce9) after user RAM feedback; mic fallback chain added.
- Session 2026-07-04 side-note: a SEPARATE Swift/WhisperKit suzune implementation exists at /Users/maverick/codex/suzune (different Claude session, bundle id com.suzune.Suzune). Do not confuse the two; watch for global-shortcut and TCC permission crossfire when both run.
- 2026-07-04 session 2: codex Swift project deleted per user. Terminal-dictation complaint root-caused: injection into terminal PROVEN working (inject_demo lands in prompt); real culprit was macOS Continuity re-grabbing the default mic for the iPhone -> silence -> silent idle. Fixed: settings.input_device pin (user's set to "MacBook Pro Microphone"), empty transcript now shows an error pill. Committed 3938eec.
- M4 DONE (55c151b): paperback app icon, README (measured claims only), Info.plist (mic usage + LSUIElement), suzune.app + dmg bundles, pushed PRIVATE to https://github.com/francisreubenr-rvu/suzune. AppleDouble files also corrupt .git/objects/pack — run find .git -name '._*' -delete if git errors appear.
- M3 remaining (deferred): settings editor UI, waveform level metering, engine-load stagger. Bundled .app has its own TCC identity — user currently runs the bare binary (inherits terminal permissions).
- M5 DONE: landing/index.html (single self-contained file, paperback theme, CSS-3D book hero, scroll transitions). Visually verified desktop (1440px) + mobile (390px) via headless Playwright screenshots on 2026-07-05; fixed a real bug found in verification (book cover subtitle text overflowed/clipped at ≤460px — added padding + a mobile font-size step).
- M6 DONE: demo video (docs/demo/suzune-demo.mp4, re-rendered 2026-07-05 by capturing docs/demo/demo-anim.html frame-by-frame with headless Chromium + ffmpeg — the animation source was already suzune-branded, only the video artifact was stale) + LinkedIn post (docs/linkedin-post.md, still held per user — not posted).
- 2026-07-05: milestone audit — confirmed M0-M6 all complete except two M3 items intentionally deferred and never revisited: waveform level metering (Overlay.tsx only shows a static pulsing dot + text label, no live audio level visualization) and engine-load stagger (ASR engine and cleanup llama-server both load back-to-back on the coordinator thread at startup, coordinator.rs Worker::new — no explicit stagger). Neither blocks normal use; flagging so they don't get silently forgotten.
- 2026-07-07, session (model cleanup + grammar/tone modes + personalization):
  - **Model cleanup**: deleted 9 confirmed-unused files from `/Volumes/1TB SSD/LM/suzune-models/` (leftover tar.gz, unwired Whisper model, 7 rejected/failed cleanup-LLM bake-off candidates) — 12G -> 1.5G. Kept parakeet-tdt-0.6b-v2-int8/, Qwen2.5-1.5B-Instruct-Q4_K_M.gguf, silero_vad_v4.onnx. Also renamed the stale local cargo target-dir (`~/.cache/cargo-targets/whispr` -> `.../suzune`, since `.cargo/config.toml` had never been updated post-rename) and killed an orphaned `dev.fude.app`-era llama-server that had been running since a much earlier session.
  - **Grammar strictness + tone**: cleanup is now a two-pass pipeline — Pass 1 (grammar, always runs) parameterized by 5 levels (Butler/Casual/Standard/Formal/Oxford, default Standard), Pass 2 (tone restyle, optional) with 5 values (Neutral default/skips-entirely, Playful, Enthusiastic, Direct, Dramatic), both through the same running llama-server (no second model, no extra RAM). New `GrammarLevel`/`Tone` enums + `build_grammar_prompt`/`build_tone_prompt` in `crates/cleanup/src/prompt.rs`; `CleanupClient::clean()` now takes a `few_shot` slice too (see personalization below). `GrammarLevel::Casual` is byte-identical to the old `SYSTEM_PROMPT_V3` (regression-tested). New Settings fields `grammar_level`/`tone`; `reload_settings` rebuilds just the client (no llama-server respawn) when only these change. New Settings UI sections reusing the existing `.segmented`/`.seg` pattern.
  - **Personalization**: new opt-in `personalization_enabled` (default false) Settings field. New `src-tauri/src/personalization.rs` module: local-only `corrections.jsonl`/`vocabulary.json` in the app config dir, populated only when the user actively corrects a dictation via the new Settings "History & personalization" section (rolling 50-entry in-memory buffer, never persisted until corrected). Corrections feed both a few-shot example selector (Jaccard + recency, K=4) and a deterministic personal-vocabulary substitution applied before Pass 1. New Tauri commands: `get_recent_history`, `submit_correction`, `list_corrections`, `clear_corrections`.
  - **Real-model verification, not just unit tests**: ran the actual 20-sample bake-off (`crates/cleanup/examples/mode_bench.rs`, links `suzune_cleanup` directly rather than duplicating prompts in Python like bench.py/bench_v31.py do) across all 5 grammar levels + 4 tones = 180 real llama-server calls. Found and fixed a real bug: the tone-restyle pass had no code-conversion guard of its own (independent prompt from Pass 1), and `tone-enthusiastic` converted a code-adjacent sample into an actual Java block — added an equivalent rule 3 to `build_tone_prompt`. Residual disclosed risk: 4/180 calls (2 of 20 samples, both literally about code, only under non-neutral tones) still occasionally leak code syntax — documented in `docs/spike-results.md`'s new "S3 follow-up" section, not silently accepted. Also found (and could not fully fix after 2 prompt-wording attempts) a narrow Butler-level bug where one specific self-correction phrasing ("X actually no Y") doesn't resolve cleanly — documented as a known residual defect, consistent with this project's existing tolerance for bounded small-model imperfections (see S3's #7/#9 notes). 8 new `#[ignore]`d integration tests in `crates/cleanup/tests/live_server.rs`, all passing against the real model.
  - **Phase 2 (LoRA fine-tuning) is designed but NOT built** — deliberately deferred as milestone **M7**: once `corrections.jsonl` reaches ~200+ entries, a manual dev-machine `mlx-lm` LoRA workflow (train on reshaped corrections -> fuse -> convert to GGUF -> evaluate against a held-out split AND a re-run of the original 20-sample bake-off before ever offering it as an opt-in `cleanup_model` choice, never a silent default swap). Needs real usage data to accumulate first — cannot be started until personalization has actually been used for a while.
  - Visual verification of the new Settings UI sections done via headless Playwright with a mocked Tauri IPC bridge (the real .app requires an interactive display session not available in this sandbox) — light mode, dark mode, and the "Fix this" -> textarea interaction all confirmed rendering correctly.
  - Docs updated: README (settings table, pipeline diagram, new "Learning from corrections" section), `docs/legal-review.md` (new section 5 on personalization data storage — opt-in/local/clearable, no new legal exposure).
